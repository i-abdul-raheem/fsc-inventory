/** Format a local Date into yyyy-mm-dd for HTML date inputs. */
export function toHtmlDateInputValue(d: Date): string {
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}`;
}
